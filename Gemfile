source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in rails_time_travel.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

# To use a debugger
gem 'pry', group: [:development, :test]

gem 'combustion', '~> 1.3'
